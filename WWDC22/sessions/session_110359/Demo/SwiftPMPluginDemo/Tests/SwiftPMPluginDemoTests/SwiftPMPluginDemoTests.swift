import XCTest
@testable import SwiftPMPluginDemo

final class SwiftPMPluginDemoTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPMPluginDemo().text, "Hello, World!")
    }
}
