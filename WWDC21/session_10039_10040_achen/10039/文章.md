# WWDC21 - Create ML 新妙用：手势分类和手部动作分类

> 作者：阿尘，资深 iOS 开发者、开源项目作者和技术管理者，对前沿技术保持浓厚兴趣，乐于分享和交流。曾开发的开源框架 RealReachability 在 Github 获得 3k stars 和 450+ forks，在 Cocoapods 被下载数万次；作为 PushKit 的早期研究者和实践者，所撰写的原创分享常年在各大搜索引擎排名前三； csdn 博客技术专家和特约编辑，文章阅读量超百万次；曾作为 InfoQ 旗下组织 StuQ 的特邀讲师视频分享过“iOS 高效开发之道”和"iOS 下的单元测试入门实战"，获得开发者们的好评。 2018 年起接触 ML/AI (机器学习/人工智能)，自学吴恩达机器学习和深度学习相关课程并获取证书，随后作为技术负责人开发了公司的虚假照片识别项目并在 App 产品中应用，取得了不错的效果。
>
> 审核：
> 
> Damonwong，iOS 开发，老司机技术周报编辑，就职于淘系技术部
>
> 五子棋，老司机技术周报编辑，主要致力于研究一站式机器学习平台 — MNN 工作台，大家可以前往 www.mnn.zone 下载

本文基于 [Session 10039](https://developer.apple.com/videos/play/wwdc2021/10039) 梳理，如需了解更多，可以浏览文末的参考来源。

## 前言

WWDC18 苹果推出了 Create ML 框架,它极大的降低了机器学习的入门门槛。通过该框架，一个没有机器学习基础的苹果开发者也可以快速训练出自己的机器学习模型并将其应用到 iOS/Mac 应用中，且其模型在大小和运行速度方面都具备一定的优势，因此在发布当日即引起了开发者的追捧。过去几年，Create ML 及其他相关技术框架（例如 Vision）也得到了长足的发展，能力逐步细化到了各个实际应用场景中。今年，苹果给我们带来了新的应用场景：区分不同的手势和手部动作，同时还支持左右手的识别。在这篇文章中，我们一起来看看今年苹果带给我们的惊喜吧。

## 背景知识：关于手、手势和手部动作

![](https://images.xiaozhuanlan.com/photo/2021/0c7aaef98088b2e4267179ba118af56f.png)

在我们深入新技术之前，我们先来聊一聊手:手是人最重要的器官之一，它由很多的骨头、关节和肌肉组成，可谓是一个工程学上的奇迹。抛开它的复杂性不提，手是新生儿与世界互动的第一个工具。在婴儿会说话之前他们就开始尝试使用简单的手部动作来表达自己的意图。在我们学会说话以后，手继续在表达信息方面扮演着重要角色。在新冠大流行的一年里，人们通过手势来拉近彼此之间的距离:

![](https://images.xiaozhuanlan.com/photo/2021/4b1af8170726211ed879689875f011c5.png)

2020年，Vision 框架引入了手势识别。它可以帮助开发者识别出画面中的手，以及每一只手所包含的 21 个关节点。如果你想识别出屏幕里是否有一只手，那么它是一个很棒的工具；但如果你想知道屏幕里的手具体在干什么，仅仅使用手势识别功能目前还无法做到。而这就是我们今天要给大家介绍的：使用 Create ML 来对手势和手部动作分类，从而获知屏幕中的手势/动作所表达的含义。举个例子，我们可以通过手势来表达和平、停止：

![](https://images.xiaozhuanlan.com/photo/2021/7fe2df74d099ebb45664d9a6460fed33.png)

通过手部动作表达跟着我、走开等等：

![](https://images.xiaozhuanlan.com/photo/2021/553b9d346c2d6d07e9b3e4f1b5a153bb.png)

将这样的能力集成进App，可以让你轻松实现类似体感的操控，是不是很酷！

## 手势分类

讲完背景我们就开始实操；相信大家早已迫不及待想跟老司机一起上车了吧。手势分类作为一个典型的机器学习分类任务，其训练用的数据是静态图片。完整的工作流包含三大步骤：

- 收集必要的数据并分类和处理；
- 使用 Create ML 训练出分类模型；
-  集成模型到项目中使用。

![](https://images.xiaozhuanlan.com/photo/2021/b4af6a0bbaf9f94c5e46cd2887394677.png)

### 步骤一：数据收集

首先你需要设计你需要识别的手势分类，同时引入一个 background 类别，用来降低误判。在 demo 演示中，苹果工程师设计了三种手势：单指、双指、五指张开，分别代表了不同的含义。分别需要为三种手势准备数十至数百张图片，作为训练的素材。图片的张数和质量很大程度上决定了训练得到的模型质量，因此，我们需要对其进行严格把关。多种手势的照片数量建议保持相对均匀（演示中每一种手势大概 500 张图），background 类别可以适当增加数量。background 类别中需要放置一些无手势或者容易混淆的过渡手势、不用识别的其他手势等等，这样就可以把它们和你关心的手势区分开。准备好图片后把它们分别放到类别命名的文件夹中：
![](https://images.xiaozhuanlan.com/photo/2021/2b0819a5143b5d587bf32b0f16213157.png)

### 步骤二：模型训练

Create ML 新增了一个手势识别分类的模版，使用起来非常容易。我们使用模版导入图片文件夹（在 Model Source 的标签下新建并导入）后就可以开始模型的训练：

![](https://images.xiaozhuanlan.com/photo/2021/501d9dff30e5bfa53963b9a5bb22525f.png)

苹果的图片任务训练都分为两步骤，其中第一步骤是特征提取，此步骤是比较耗时的。选取合适的迭代次数和其他的一些训练设置后，我们点击训练，经过一段时间的等待，不出意外我们就可以得到一个训练好的模型了。

![](https://images.xiaozhuanlan.com/photo/2021/bf6b87dd75cecc79575f0b35d7e069be.png)

在 Create ML 中，预览是一个非常实用的功能；通过它我们可以轻而易举的验证我们的成果。在示例中，工程师通过摄像头演示了几个手势，可以看到屏幕中实时的显示出检测到的手势并展示了分类的置信度；这看起来相当酷，也非常实用。

![](https://images.xiaozhuanlan.com/photo/2021/20c46bbefc7cbc5b16085fb085e00504.png)

### 步骤三：应用集成

一位来自 Vision 框架团队的苹果工程师通过一个 ARKit 相关的 demo 来演示该模型的能力。具体而言，她通过摄像头对手势的实时监测，在手指上加特效（类似魔法光束，看起来很酷炫）；一根手指、两根手指、整个张开的手掌这三个手势分别是不同的特效，并且特效还可以跟随手指的位置移动。这个 demo 很好的利用了以下的技术能力：

- 利用ARKit来获取摄像头的实时图像帧（也可以使用其他方法，比如AVCaptureOutput来实现类似的功能）；
- 利用Vision框架来实时的识别和跟踪手掌；
- 利用CreateML训练得到的模型来识别手势。


下面我们就具体看下核心代码的实现。
在摄像头的实时图像帧中识别手部：

```swift
func session(_ session: ARSession, didUpdate frame: ARFrame) {
	// 通过此方法获取实时图像帧，并进行相应的处理。
    let pixelBuffer = frame.capturedImage
    // 对图像中的手进行识别 
    let handPoseRequest = VNDetectHumanHandPoseRequest()
    handPoseRequest.maximumHandCount = 1
    handPoseRequest.revision = VNDetectHumanHandPoseRequestRevision1

    let handler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, options: [:])
    do { 
        try handler.perform([humanBodyPoseRequest]) 
    } catch {
        assertionFailure("Human Pose Request failed: \(error)")
    }

    guard let handPoses = request.results, !handPoses.isEmpty else {
        // No effects to draw, so clear out current graphics
        return
    }
    let handObservation = handPoses.first
```

使用模型来对手势进行判定：

```swift
if frameCounter % handPosePredictionInterval == 0 {
    
    guard let keypointsMultiArray = try? handObservation.keypointsMultiArray()
        else { fatalError() }
    // 核心代码，使用model进行predict；输入是keypointsMultiArray。
    let handPosePrediction = try model.prediction(poses: keypointsMultiArray)
    let confidence = handPosePrediction.labelProbabilities[handPosePrediction.label]!
    // 置信度0.9以上才进行特效的渲染。
    if confidence > 0.9 {
        renderHandPoseEffect(name: handPosePrediction.label)
    }
}

// 特效渲染方法实现。
func renderHandPoseEffect(name: String) {
    switch name {
    case "One":
        if effectNode == nil {
            effectNode = addParticleNode(for: .one)
        }
    default:
        removeAllParticleNode()
    }
}
```
	
获取食指尖的位置作为魔法光束的锚点：

```swift
let landmarkConfidenceThreshold: Float = 0.2 // 这里选取的阈值较低。

let indexFingerName = VNHumanHandPoseObservation.JointName.indexTip

let width = viewportSize.width
let height = viewportSize.height

if let indexFingerPoint = try? observation.recognizedPoint(indexFingerName),
   indexFingerPoint.confidence > landmarkConfidenceThreshold {
    // 坐标转换
    let normalizedLocation = indexFingerPoint.location
    indexFingerTipLocation = CGPoint((x: normalizedLocation.x * width,
                                      y: normalizedLocation.y * height))
} else {
    indexFingerTipLocation = nil
}
```
	
其中最核心的部分，检测手势的流水线示意图如下：

![](https://images.xiaozhuanlan.com/photo/2021/681edb1ce44f7bf1074295204ea2efca.png)

除此之外，Vision 框架中新增了一个枚举属性可以用来区分左右手。这个能力的检测是每个手独立的（左手不依赖右手，所以如果有误判的话可能两只都是左手或者右手，哈哈）。通过这个 api，在应用中我们可以实现两只手的分别控制，使用起来很简单，却很使用。

区分左右手的代码示例：

```swift
// Working with chirality

let handPoseRequest = VNDetectHumanHandPoseRequest()
try handler.perform([handPoseRequest])
let detectedHandPoses = handPoseRequest.results!
// hand.chirality, 左右手
for hand in detectedHandPoses where hand.chirality == .right {
    // Take action on every right hand, or prune the results
}
```

## 手部动作的分类

手势分类应用讲完了我们下面再聊聊手部动作的分类。 WWDC20 有一个 session 给大家介绍了如何通过 Create ML 训练一个动作分类器，手部动作的分类只是训练所使用的素材不同，其他的过程很相似，因此建议大家如果要自己上手训练手部动作分类模型的话，也可以先找到那个 session 来回顾下（参看文末的参考来源连接）。

动作和手势的不同点在于一个是手势的序列，一个是单个手势；对应来说就是视频和单帧图像的区别。对于视频的素材收集相对来说更难，数据量也更大。
素材的使用有两种方式，一种是和之前的手势识别类似，只是素材改为视频：

![](https://images.xiaozhuanlan.com/photo/2021/4daf4279d124fd3a67c26c74baf6d901.png)

另外义重在文件夹中需要用到 .csv 或者 .json 来存放一些视频相关的辅助信息（ WWDC20 有介绍，这里就不展开说了）：
![](https://images.xiaozhuanlan.com/photo/2021/ca10135676e18a52ad47225894fa663f.png)

在展示的 demo 中，苹果演示了通过动作触发的新魔法，效果看起来也相当酷：

![](https://images.xiaozhuanlan.com/photo/2021/dd3af29e8dfd5e681fdd0ff5cd905097.png)

下面我们介绍下如何使用训练得到的模型来在 App 中实现动作检测。

使用FIFO(先入先出)的队列来累计存放手势，从而实现特定手部动作的识别：

```swift
var queue = [MLMultiArray]()
// ...
//控制数据填充的速度
frameCounter += 1
if frameCounter % 2 == 0 {
    let hands: [(MLMultiArray, VNHumanHandPoseObservation.Chirality)] = getHands()
    //判断左右手，仅右手才进行相关操作
    for (pose, chirality) in hands where chirality == .right {
        queue.append(pose)
        queue = Array(queue.suffix(queueSize))
        queueSamplingCounter += 1
        if queue.count == queueSize && queueSamplingCounter % queueSamplingCount == 0 {
            // 组装输入
            let poses = MLMultiArray(concatenating: queue, axis: 0, dataType: .float32)
            // 执行分类操作
            let prediction = try? handActionModel?.prediction(poses: poses)
            guard let label = prediction?.label,
              let confidence = prediction?.labelProbabilities[label] else { continue }
            if confidence > handActionConfidenceThreshold {
                DispatchQueue.main.async {
                    self.renderer?.renderHandActionEffect(name: label)
                }
            }
        }
    }
}
```
	

输入的维度多了一个 45，这个 45 是怎么来的呢？它实际和我们选取的动作长度有关： 45 等于 1.5 乘以 30 ， 1.5 是动作的时长，30 是帧速率。可以看到上面的代码里每 2 帧采样 1 次，是因为 demo 中的代码 ARSession 刷新频率是每秒 60 次，通过这种采样方式才可以和我们的模型对应上。
![](https://images.xiaozhuanlan.com/photo/2021/38616683d26828b468b8ec98455130e5.png)

另外一个值得注意的是性能问题：可以想象到，这种基于动作序列的预测性能上的代价应该远大于单个手势识别。因此，多久执行一次动作序列的预测成为了一个关键决策。你需要在应用流畅性和检测的实时性上进行一个平衡，选取合适的检测频率。

## Tips

基于实际项目经验，我们再送给大家几个小Tip：

- 手到镜头的距离，尽量不要超过3.5米；
- 光线不要太强也不要太暗；
- 最好不要戴手套，这会让识别变得困难；
- 保证充足、高质量的训练数据，包括背景分类的数据。

## 总结

最后总结一下，这篇文章介绍了四点内容：

- 手势和手部动作介绍；
- 两个CreateML新模版，使用它们来进行新模型的训练；
- 把训练到的模型集成到App中使用；
- 左右手检测，在App中同时使用多个模型。

![](https://images.xiaozhuanlan.com/photo/2021/8b8badcaa5cc4a3d3e4fa7b2f9ad49ee.png)

希望大家看了这篇文章，可以开发出更有创造力的应用，谢谢！

## 关注我们

我们是「老司机技术周报」，一个持续追求精品 iOS 内容的技术公众号。欢迎关注。

![](https://images.xiaozhuanlan.com/photo/2021/71326704716a5f65a020bfcc08f409a3.)

**关注有礼，关注【老司机技术周报】，回复「WWDC」，领取 《WWDC20 内参》**
