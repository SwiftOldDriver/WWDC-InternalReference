# WWDC21 10041 - 使用Vision提取文档里的数据

基于 [Session 10041](https://developer.apple.com/videos/play/wwdc2021/10041/) 梳理

原文标题 - **Extract document data using Vision**



## 前言

看懂本文，你最好具备基础的 Vision 相关知识：

1. 条形码识别 VNDetectBarcodesRequest
2. 文本识别 VNRecognizeTextRequest
3. 矩形框识别 VNDetectRectanglesRequest



## 简介

本文主要回顾了往年 Session 中的 Vision 条形码识别、文本识别（OCR），然后是本次新增加 DocumentSegmentation（文档分割）。

目前， Vision 已经广泛应用于图像分析，支持各方面的需要：

<img src="https://cdn.nlark.com/yuque/0/2021/png/21804904/1624175022723-caed428b-0a02-4656-a301-fa86c6264c76.png?x-oss-process=image%2Fresize%2Cw_1198" alt="Vision Focus" style="zoom:80%;" />

1. 运动（动作分类，手势辨认、对象跟踪等）
2. 辅助（OCR 识别，帮助视觉障碍人士读取图片上的信息）
3. 脸部识别、身体分割
4. 健康 （条形码识别，分析人的姿势以做出摔倒的概率预测等）
5. 拍照 （比如系统相机里的人像模式，检测出脸部和环境，从而聚焦在脸部）
6. 安全
7. 文档

而，本文聚焦在第7种，文档方面的新应用



## 回顾及更新

### 1. 条形码

Vision 的条形码检测器特性：

- 支持识别一维、二维的条形码
- 支持一次性识别一张图片中的多个条形码
- 支持一次性识别多种类条形码

#### 更新

此次苹果更新了算法，增加了 **VNDectectBarcodesRequestRevision2**，支持更多种类的条形码检测：

![新的条形码算法](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624176325092-26e6e2f6-77eb-410c-bef2-89f45e9195f7.png)

其中，**microQR** 可能是对我们最有用的！如果你只想简单用一个 URL 生成二维码，又想生成的图片占更小的面积，那么就用它吧！

需要**注意**的是，这次算法**变更更大**的地方在于，它改变了一些行为：

![](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624177129818-eb6ab5dc-ae1f-48f7-b1d5-d2dfe6d36bb5.png)

旧版本算法（Revision1）：不管你是否设置了 ROI ，得到的结果中的 BoundingBox 总是基于原图的左下角

新版本算法（Revision2）：得到的结果中的 BoundingBox，是基于 ROI 的左下角为坐标系的（当没有设置 ROI 的时候，和旧算法一致）

> ROI：Region Of Interest，感兴趣的区域，图上的黄色区域。通过它，你可以在检测某张图片时候，只聚焦在其中一部分，这往往可以提高检测的速度，减少耗时。
>
> BoundingBox：检测到的物体的几个顶点组成的，最少能包围住顶点的矩形边框，图上的红色区域。

所以，在使用 Vision 的时候，我们最好**明确的、显式**写上当前使用的版本！ 如果不写，会默认使用最新的版本，这样苹果一旦在新系统发布新版本算法时候，你的 app（哪怕是用户之前安装的）可能得到莫名其妙的结果。

#### 如何工作的？

##### 一维码

![一维码](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624178687782-63c5ad1d-2cff-4cb1-a5c8-91500715e410.png)

一维码是按行扫描的，而每行的信息是一样的，所以你会得到多个一样的检测结果。当然，我们不需要人工去重，苹果帮我们做了，直接读取 **payload**，就是真正的数据了。

一般商品的包装带上都有这种码。

##### 二维码

![二维码](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624179283718-b979abdb-b1e8-4e93-8c0e-1c1f031b0353.png)

二维码是直接按单元扫描的，每个码有且仅有一个 boundingBox 。 直接读取 **payload** 就可搞定了。另外如果码中内容是 URL，需要转义 % 符号的，可以用 **NSDataDetector**

它是我们最常见的码，比如微信、支付宝等等的 `QR Code`

#### Demo

![条形码Demo](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624180018217-4a488d6a-b808-4eca-92a8-29ff787e6c70.png)

样板式请求代码哈。其中，这两个地方需要注意：

```swift
// 明确指名使用的算法版本
detectBarCodesRequest.revision = VNDectectBarcodesRequestRevision2 
// 需要检测哪种类型的条形码，可传入多个。 若传入[]，则代表需要检测所有类型
detectBarCodesRequest.symbologies = [.codabar]
```

### 2. 文本识别

Vision 的文本识别也可叫 OCR 识别，于2019年的 WWDC 中首次引入。它有 **Fast**(快速) 和 **Accurate**(精确) 两种工作模式：

![compare](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624190488060-8f370776-a48d-4eae-8f06-11574c92005b.png)

两种模式，都需要经过  `识别器识别 -> 语言矫正 -> 得到识别的文字`

两种模式有何区别？主要是识别器的不同。Fast 模式下使用的是拉丁字符识别器，Accurate 下使用的是基于机器学习的识别器。 而这一个区别也导致了，像**中文**这种复杂的非拉丁字符组成的文字，只能用 Accurate 模式！而**英语**则可以从两种模式中选择。

```swift
        let textRequest = VNRecognizeTextRequest()
				// 设置工作模式 .fast 或者 .accurate
        textRequest.recognitionLevel = .accurate
				// 是否启用语言矫正
        textRequest.usesLanguageCorrection = false
```

另外， 是否启用语言矫正，也对性能产生影响，根据你使用的情况，如果能**关闭**，识别速度可以更快。

举个例子，如果现在你有一个 crash 文件，里面的是看门狗杀死的代码 `0x8badf00d`，把 `0x8badf00d` 截成图片拿去识别。如果你开启了语言矫正功能，则会被 Vision 识别成 0x8bad**food**，因为 0 像 o，food 是有意义的单词，而 f00d 是没有的！这个时候，如果要识别出 f00d，就需要关闭自动矫正。



##### 关于语言的设置

```swift
          let supportLanauages = try? textRequest.supportedRecognitionLanguages() // iOS 15才有的api
          // ["en-US", "fr-FR", "it-IT", "de-DE", "es-ES", "pt-BR", "zh-Hans", "zh-Hant"]
          textRequest.recognitionLanguages = ["zh-Hans", "zh-Hant"]
```

在识别中文时候，需要手动设置语言， 我们可以通过 `supportedRecognitionLanguages` 得到当前支持的所有语言，然后进行设置。语言的顺序非常重要。Vision 默认使用你指定的第一种语言的模型先进行识别，失败后才会用第二种、第三种等等。

现在我们有这么一张图片需要文字识别：

<img src="https://cdn.nlark.com/yuque/0/2021/png/21804904/1624192138271-a95bce31-e228-49ec-967e-deaf700b7c67.png" alt="Text_Demo" style="zoom:50%;" />

如果 `recognitionLanguages` 设置成 `["en-US", "zh-Hans"]`，识别出来的是：

​			**VR77, tER**

​			**Hello World**

​			**Grüsse aus Cupertino**

很明显，中文错误了，而其他两个拉丁文的没有影响。 把 `recognitionLanguages` 设置成 `[zh-Hans", "en-US"]`，则识别出来的是正确的！ 所以顺序是很重要的。



### 3. 文档检测

在今天的新主题 **文档分割请求** `VNDetectDocumentSegmentationRequest` 出来之前，苹果已经有了一个便捷的文档工具 `VNDocumentCameraViewController`。 它是一个类似于 UIImagePickerController 一样的高度封装工具，都是 UIViewController。 通过它，你能进入到一个文档扫描拍摄的页面，可以连续扫描多张，系统为你进行自动矫正（有时候需要人工选择四个边角）。

```swift
// 需要导入这个
import VisionKit

if VNDocumentCameraViewController.isSupported {
    let vc = VNDocumentCameraViewController()
    vc.delegate = self
    present(vc, animated: true, completion: nil)
} else {
    print("current device doesn't support document camera")
}
```

之后也是通过 delegate 进行结果回调，和 `UIImagePickerController` 简直一模一样：

```swift
// MARK: - <VNDocumentCameraViewControllerDelegate>

extension VisionKitDemoVC: VNDocumentCameraViewControllerDelegate {
    // 成功的回调
    func documentCameraViewController(_ controller: VNDocumentCameraViewController, didFinishWith scan: VNDocumentCameraScan) {
        controller.dismiss(animated: true, completion: nil)
        // 可能扫描了多张
        for pageIndex in 0..<scan.pageCount {
            let image = scan.imageOfPage(at: pageIndex)
						// 对图片进行其他操作...
        }
    }
    
  	// 用户取消了
    func documentCameraViewControllerDidCancel(_ controller: VNDocumentCameraViewController) {
        controller.dismiss(animated: true, completion: nil)
    }
    
    // 错误处理
    func documentCameraViewController(_ controller: VNDocumentCameraViewController, didFailWithError error: Error) {
        controller.dismiss(animated: true, completion: nil)
    }
}
```

有兴趣的同学，可以去看之前的 Session：[Text Recognition in Vision Framework](https://developer.apple.com/videos/play/wwdc2019/234/)，16:00分钟的地方开始哦。 

OK。接下来进入今天真正的全新内容！



## 文档分割

> 注意：仅 iOS 15.0+ 可使用!!! 

`VNDetectDocumentSegmentationRequest`  文档分割，把图片中的文档部分切割出来。 

我们知道，从图片中切出文档的部分，用 RectangleDector 后再特殊判断也是可以的，那么为什么还要用新的 DocumentSegmentation 呢？

`VNDetectDocumentSegmentationRequest` 和 `VNDetectRectanglesRequest` 的不同点是，RectangleDetector 对于画面中能检测到的矩形框，都可能是识别结果（视你的最小检测物体比例和最大识别个数设置），而 DocumentSegmentation 只会去识别画面中的文档部分！ 这样就少了特殊处理的环节，且误判更少。

![Document Segment vs Rectangle Detector](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624196204021-f9c380f1-0bfd-41a3-92db-29b94138406c.png)

如上图，同一张图片，左边的 DocumentSegmentation 只能得到一个结果，就是文档本身； 而右边的 RectangleDetector 得到了5个矩形框，它可以是嵌套的！

在底层技术以及细节上，对比如下：

![Details: Document Segment vs Rectangle Detector](https://cdn.nlark.com/yuque/0/2021/png/21804904/1624196767830-bb43b551-52a0-47b7-aa94-d5ba7da36127.png)

DocumentSegmentation 基于机器学习，运行在神经引擎，GPU or CPU上的；而 RectangleDetector 只能是在 CPU 上运行传统算法。DocumentSegmentation 能识别出非矩形形状的文档、标签、符号等，而 RectangleDetector 只能基于边缘，查找交集。DocumentSegmentation 现在能够提供mask和顶角点坐标，而 RectangleDetector 只提供顶角点坐标。

另外，上面讲的 `VNDocumentCameraController` 现在已经把内部的核心检测换成了 `DocumentSegmentation`！



#### Demo

1. 获取图片中的文档

```swift
func recognize(image: UIImage) -> VNRectangleObservation? {
    guard let ciImage = image.ciImage ?? CIImage(image: image) else {
        print("创建ciimage失败!")
        return nil
    }

    let documentSegmentationHandler = VNImageRequestHandler(ciImage: ciImage, orientation: CGImagePropertyOrientation(image.imageOrientation), options: [:])
    let documentSegmentationRequest = VNDetectDocumentSegmentationRequest()
    documentSegmentationRequest.revision = VNDetectDocumentSegmentationRequestRevision1
    try? documentSegmentationHandler.perform([documentSegmentationRequest])

    guard let result = documentSegmentationRequest.results?.first, result.confidence > 0.7 else {
        print("未检测到文档")
        return nil
    }
    return result
}
```

之后通过得到的 observation 中的四个点（其实是 **VNRectangleObservation** 的属性，VNDetectDocumentSegmentationRequest 执行返回的结果和 RectangleDetector 得到的结果是同一个类），进行线的绘制

```objc
API_AVAILABLE(macos(10.13), ios(11.0), tvos(11.0))
@interface VNRectangleObservation : VNDetectedObjectObservation
  
@property (readonly, nonatomic, assign) CGPoint topLeft;
@property (readonly, nonatomic, assign) CGPoint topRight;
@property (readonly, nonatomic, assign) CGPoint bottomLeft;
@property (readonly, nonatomic, assign) CGPoint bottomRight;

@end
```

我尝试了几张图，结果如下**绿色线部分**（识别到的4个顶角组成的线）：

<img src="https://cdn.nlark.com/yuque/0/2021/png/21804904/1624200257500-fe2277f5-40a3-40cb-8ec5-5b368cc75236.png" alt="DocumentSegmentation Demo_1" style="zoom: 40%;" />

<img src="https://cdn.nlark.com/yuque/0/2021/png/21804904/1624200286844-ae54c9a1-4aee-4b67-8794-e71a1fd54d1f.png"  alt="DocumentSegmentation Demo_2" style="zoom:40%;" />

2. 裁剪、矫正

   我们可以把这部分区域或者 boundingBox 的区域裁剪出来，然后进行矫正。一般我们是把 boundingBox 的区域用 CIImage 的 `crop(to:)` 直接裁剪，然后再用 CIFilter 直接视觉矫正，核心代码如下：

```swift
let imageSize = ciImage.extent.size
let boundingBox = result.boundingBox.scaled(to: imageSize)
let correctedImage = ciImage
    .cropped(to: boundingBox)
    .perspectiveCorrect(by: result, imageSize: imageSize)


extension CIImage {
  
    /// 视觉矫正， 把 distort 的弄正
    /// - Parameter observation: Vision返回的结果
    /// - Returns: 新的CIImage
    func perspectiveCorrect(by observation: VNRectangleObservation, imageSize: CGSize?) -> CIImage {
        let imageSize = imageSize ?? self.extent.size
        let newImage =
        applyingFilter("CIPerspectiveCorrection", parameters: [
            "inputTopLeft": CIVector(cgPoint: observation.topLeft.scaled(to: imageSize)),
            "inputTopRight": CIVector(cgPoint: observation.topRight.scaled(to: imageSize)),
            "inputBottomLeft": CIVector(cgPoint: observation.bottomLeft.scaled(to: imageSize)),
            "inputBottomRight": CIVector(cgPoint: observation.bottomRight.scaled(to: imageSize))
        ])
        return newImage
    }
}
```

结果如下：

<img src="https://cdn.nlark.com/yuque/0/2021/png/21804904/1624204782722-7edeb40f-b348-42ba-a26a-72e04ab595de.png" alt="DocumentSegmentation Demo_1 corrected" style="zoom:50%;" />

3. 后续处理

如果图片中有你需要的二维码或者文字，则继续把上一步取得的结果 correctedImage，作为下一步的输入：

```swift
// 二维码检测识别
let barcodesRequest = VNDetectBarcodesRequest()
barcodesRequest.revision = VNDetectBarcodesRequestRevision2
barcodesRequest.symbologies = [.qr]

// ocr
let textRequest = VNRecognizeTextRequest()
textRequest.revision = VNRecognizeTextRequestRevision2
textRequest.recognitionLevel = .accurate
textRequest.recognitionLanguages = ["zh-Hans", "zh-Hant"]

// 使用correctedImage创建新的handler
let requestHandler = VNImageRequestHandler(ciImage: correctedImage, options: [:])
// 开始识别、检测
try? requestHandler.perform([barcodesRequest, textRequest])

// 结果处理 - 二维码检测识别 
if let observation = barcodesRequest.results?.first,
   let payload = observation.payloadStringValue {
    print(payload)
}

// 结果处理 - ocr
if let observation = textRequest.results?.first,
   let canditate = observation.topCandidates(1).first {
    print(canditate.string)
}

// 控制台输出：
// http://weixin.qq.com/r/jHWvt6XEQPg-reHU9yCe
// 功能介绍
```

这里，我们一次性用 requestHandler 执行了两个请求，如果图片中有你需要的其他信息，你可以执行更多的请求。

> 实际应用中，记得不要放在主线程处理。Vision 的每一次检测都是比较耗时的。特别是 ocr！



## 总结：

此 Session 内容是比较少的，着重在于回顾之前的 Vision 相关知识，然后更新部分东西。 不过，如果你需要从文档里提取信息，DocumentSegmentation 会是不错的第一步！



## 后记

如果你对 Vision 感兴趣，可以去这些 Session 继续深入了解：

1. [Vision Framework: Building on Core ML](https://developer.apple.com/videos/play/wwdc2017/506)
2. [Understanding Images in Vision Framework](https://developer.apple.com/videos/play/wwdc2019/222)
3. [Text Recognition in Vision Framework](https://developer.apple.com/videos/play/wwdc2019/234/)
4. [Explore Computer Vision APIs](https://developer.apple.com/videos/play/wwdc2020/10673)
5. [Vision with Core ML](https://developer.apple.com/videos/play/wwdc2018/717)
6. [Object Tracking in Vision](https://developer.apple.com/videos/play/wwdc2018/716)
