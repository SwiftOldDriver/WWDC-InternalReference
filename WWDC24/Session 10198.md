# Session 10198 运行、暂停、检查：探索如何使用LLDB进行有效调试

## 概述
本文以 WWDC24-10198 为主线，综合了 WWDC19-24 期间多个和调试主题相关的 Session，向大家介绍如何使用 Xcode IDE
和 LLDB 的最新能力，进行有效和高效的调试。同时介绍了如何处理使用相关工具碰到的常见问题和一些高级技巧。

整个文章分为三个部分。第一部分是定义调试的一般模型；第二部分重点介绍 LLDB 调试的多个不同能力，包含代码回溯、断点和变量表达式
评估( p 命令)等能力，这里我们除了讨论面向不同调试场景，如何应用断点设置的高级技巧外，还将深入探讨变量表达式评估背后的运作机制
和不同评估命令的异同；第三部分我们将讨论调试中碰到的恼人问题，例如如何解决模块无法符号化造成断点决议以及表达式评估不可用的问题，
我们还将介绍 Swift 调试遇到的一些特殊问题，最后会稍微展开介绍 LLDB 命令行的常见命令和自定义脚本扩展能力。

## 调试的模型
img: Session%2010198/1.png
当调试程序中的问题时，我们通常会在程序出现错误的时间点得到信息。这可能是崩溃，显示不正确的值，甚至是程序挂起。在程序执行开始 到问题发生之间，执行了错误的代码，我们的目标是要找到这段错误的代码。 通常可以通过在不同的时间点检查程序的状态来找到错误，每次检查都会使我们更接近问题代码。我们有两种方法来解决这个问题。第一种是使用代码记录的日志，来定位错误发生的时间节点和对应的代码位置，这需要开发人员的预见性来决定要记录哪些有用的信息。如果日志记录偏少，或者关键节点缺乏日志，或者因为性能原因关闭了高频的调试日志，就可能导致搜索范围过大，无法高效的定位问题。 我们要讨论的是第二种方法，就是打印调试技术，这可能是我们所有人学习的第一个调试方法。 通过打印调试，我们在程序中插入打印语句，重新编译代码，运行程序，并重现错误。最后，我们检查打印的消息。如果我们想打印新的内容，我们需要重复整个过程。 最终，我们将检查足够的程序状态以修复错误。但是这种方式效率低下，因为需要不断的重新编译和运行程序。奥对了，我们还不能忘记在最后的 Release 版本，移除那些打印语句。而 LLDB 调试器可以帮助优化这个流程，在程序运行时检查程序状态，而不需要重新编译和运行程序。当使用 LLDB 调试器时，调试模型看起来像是如下的样子的，它将使用接下来要介绍的 LLDB 两个关键的调试能力：断点和变量表达式评估。


## LLDB 调试能力
### 示例工程
为了能够介绍如何在实际应用中使用LLDB的调试能力，我们创建了一个示例工程Destination video。这个应用有一个子功能是点击按钮，把视频加入“Watch Later”列表中去。现在看起来在某种情况下点击按钮，会引起崩溃。我们需要定位这个崩溃的原因。
img: Session%2010198/2.png
### 代码回溯
当我们在处理崩溃问题时，通常会得到一个崩溃日志。这个崩溃的日志如下显示。
img: Session%2010198/3.png
TODO：通过这个例子简单介绍下崩溃日志的格式。

为了能够分析崩溃的代码位置和调用路径，我们需要使用 LLDB 的*代码回溯*能力。它的原理是读取分析崩溃日志，根据运行App的符号信息，定位崩溃发生的代码和方法调用路径。当使用Xcode打开崩溃日志时，如同在本地调试的崩溃现场一样，可以直接在栈帧和源代码面板，定位到方法级别的调用链和源代码行信息。为了让 LLDB 调试器能够定位到正确的源代码行号，请确保崩溃日志与创建崩溃日志的应用版本相同，并且其dSYM捆绑包是可用的。具体来说，必须保证本地工程通过git checkout到相同的commit id。由于dSYM捆绑包一般是在 Continuos Integration(CI)编译打包生成，因此可以通过CI系统查找对应版本的dSYM下载得到。

TODO：初步分析从这个崩溃日志，能得到什么初步的信息。

### 断点
我们可以在相关代码行设置断点，来分析用户点击按钮后的程序运行状态。在创建按钮的代码行设置一个行断点。lldb会将行断点解析为三个不同的位置，这表明三个不同的代码路径会在该断点停止。这种情况在设置行断点中是非常常见的。为了进一步理解创建按钮时的程序状态，我们Disable其他两个断点，通过 p 命令检查“Watch Later”列表大小（之后我们会详细介绍这个命令）。顺便说一句，如果我们想探查按钮事件执行时的状态，也可以使用之前Disable的方式来做；不过，Xcode13开始提供了一个更简便的方式，就是直接在对应的闭包代码前设置列断点，这样调试器就能够精确的在执行到闭包的时候停下来。
img: button_ctor.png
img: button_closure.png

每次断点触发，在LLDB console通过p命令，手动查看变量状态较为低效，有没有办法自动化这个流程呢。我们可以通过定义断点的触发Action，来自动化这个流程。把 p watchItemList.count 加入到Action中去。有两种方法来设置。一种是通过在断点的面板，设置动作栏，注意要全选Continue After Action，这样执行完动作后就会继续执行代码；第二种就是在LLDB console通过命令行来设置，代码如下
code：2.code
重新运行APP后，Console会自动打印加入到队列的视频信息了。这样的能力就类似在相关的代码加入了打印语句去探测程序的状态，但是好处是不用每次重新编译了！
同时我们注意到这些LLDB的调试手段，都可以通过命令行来完成，我们将在第三部分详细介绍常用的LLDB调试命令和高级技巧。

code: 3.code
我们注意到，这个断点被重复触发，因为它在一个循环内。对这种高触发断点，我们可能仅对满足某些条件下触发的情况感兴趣。对此，有以下三种技术来处理高触发断点
- 仅当某个表达式满足触发，可以在断点设置中增加条件表达式
- 仅当之前某个方法（假设为A）也被调用时触发（假设为B），可以在A的断点动作中追加临时断点和自动执行，临时断点设置为在B中断
- 循环中，忽略前N次遍历，在N+1次触发
- code: 4.code
在这个代码片段，它将遍历集合中的视频，如果视频位于远程位置，则加载视频并处理它们。我们可能只对加载远程媒体函数（loadRemoteMedia）感兴趣，仅当视频非常长时停止。为此，可以使用以上提到的技术#1和#2，来进行设置。 在我们的示例中，我们可以修改断点，以仅在当前视频超过60秒时才停止程序。 在Xcode中，我们将右键点击断点，导航到编辑断点，并填充条件字段。 为了保证只对调用了loadRemoteMedia函数的执行，中断在之后的processVideo，可以使用tbreak命令，在loadRemoteMedia上设置自动继续断点，并带有在processVideo上创建临时断点的动作。 
img: high-firing-#1-#2.png
需要指出的是，以上的处理技术，虽然减少了断点触发的时机，但是由于每次都会判断断点条件，因此会显著影响APP运行速度。如果想要解决这个问题，需要修改代码，在满足中断条件下，手动触发SIGSTOP信号，触发中断。
代码示意如下。
code: raise_sigstop.code
让我们看看我们程序的另一个部分：接受JSON解码器的Video构造函数，怀疑其中一个try语句有问题，但如果我们在每次调用Video构造函数时都中断，我们将会遇到问题：应用程序中有太多的视频！幸运的是，我们涵盖了可以处理这种高触发断点的技术。我知道我的新视频是应用程序中的第12个视频，所以我可以给这个断点应用一个忽略计数。 但是这次，让我们尝试一些新的东西：Swift Error断点。 这种类型的断点指示lldb在抛出Swift错误时立即停止应用程序。让我们点击继续！程序在我们尝试解码imageName JSON键的Error断点处停止。 那里存在问题。 让我们使用回溯返回到较早的帧，我们可以使用p command做一些编程来弄清楚这个data数组中存在多少个imageName，通过检查count属性发现只有12个名为imageName的键，但是我们预期应该有13个image。 通过查看JSON文件，我们发现在拼写其中一个imageName时打错了字。这就是问题的真正原因！


需要指出的是，除了之前介绍的行、列断点以及Swift错误断点之外，还包含以下几种类型的断点
- 符号断点
- 异常断点

有时候，你并没有问题发生模块的源代码；或者问题并不发生在确定的代码行，而是在访问某个变量时才出问题，这时候可以使用符号断点来定位相关的疑似问题代码。为了确认相关符号是否存在，可以使用以下LLDB代码来确认
code: image_lookup.code
虽然使用IDE添加符号断点很方便，我更喜欢使用LLDB console添加，方法如下
code: lldb_console_add_symbolic_bp.code

TODO：异常埋点


### P 和 PO命令

